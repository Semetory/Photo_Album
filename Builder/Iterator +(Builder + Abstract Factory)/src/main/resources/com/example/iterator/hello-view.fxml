<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.iterator.HelloController"
            prefWidth="900" prefHeight="700">

    <top>
        <HBox spacing="15" alignment="CENTER_LEFT" style="-fx-padding: 15;">
            <Button text="📁 Открыть папку" onAction="#chooseDirectory"
                    prefWidth="150" prefHeight="40"/>
            <VBox spacing="3">
                <Label text="Формат файлов:" style="-fx-text-fill: #666;"/>
                <ComboBox fx:id="formatCombo" prefWidth="120"/>
            </VBox>
        </HBox>
    </top>

    <center>
        <StackPane>
            <ImageView fx:id="imageView"
                       fitWidth="800"
                       fitHeight="550"
                       preserveRatio="true"
                       style="-fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 10, 0, 0, 0);"/>
            <Pane fx:id="emotionPane"
                  style="-fx-background-color: transparent;"/>
        </StackPane>
    </center>

    <bottom>
        <VBox alignment="CENTER" spacing="15" style="-fx-padding: 20;">
            <Label fx:id="statusLabel"
                   text="Выберите папку с изображениями. Клик - добавить эмоцию | Ctrl+Клик - удалить"
                   style="-fx-font-size: 14px; -fx-font-weight: bold;"/>

            <HBox alignment="CENTER" spacing="30">
                <Button text="◀ Предыдущее" onAction="#prevImage"
                        prefWidth="140" prefHeight="40"/>

                <VBox alignment="CENTER" spacing="10">
                    <Button text="▶ Старт" onAction="#startSlideShow"
                            prefWidth="100" prefHeight="35"
                            style="-fx-font-size: 13px; -fx-background-color: #4CAF50; -fx-text-fill: white;"/>
                    <Button text="⏹ Стоп" onAction="#stopSlideShow"
                            prefWidth="100" prefHeight="35"
                            style="-fx-font-size: 13px; -fx-background-color: #f44336; -fx-text-fill: white;"/>
                </VBox>

                <Button text="Следующее ▶" onAction="#nextImage"
                        prefWidth="140" prefHeight="40"/>
            </HBox>
        </VBox>
    </bottom>

</BorderPane>